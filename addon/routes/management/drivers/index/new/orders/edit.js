import Route from '@ember/routing/route';
import { inject as service } from '@ember/service';


export default class ManagementDriversIndexOrdersEditRoute extends Route {
    // @service store;
    // @service currentUser;
    // @service notifications;

    // templateName = 'management.drivers.index.new.details.new';
    // model({ public_id }) {
    //     return this.store.queryRecord('driver', {
    //         public_id,
    //         single: true,
    //     });
    // }

    // async setupController(controller, model) {
    //     controller.driver = model;
    // }
}
